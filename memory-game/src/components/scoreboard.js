import React, { useState, useEffect } from "react";

const Scoreboard = props => {
  const [score, setScore] = useState(0);
  const [cardArray, setCardArray] = useState([]);
  useEffect(()=>{
    if (cardClicked === newCard){
      console.log("ss")
    }
  })
}
export default Scoreboard